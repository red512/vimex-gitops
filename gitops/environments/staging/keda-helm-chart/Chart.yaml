apiVersion: v2
name: keda-helm-chart
description: KEDA autoscaler for event-driven scaling
version: 0.1.0
appVersion: "2.11.2"
dependencies:
  - name: keda
    version: 2.11.2
    repository: https://kedacore.github.io/charts
    # Add this to ensure CRDs are installed
    alias: keda